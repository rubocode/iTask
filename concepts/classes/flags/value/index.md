> [flags](../)

![banner](/iTask/photos/banner.png)

## Value

The value of a thing is the value of its outcome — the value of taking care of that thing to accomplish something.

In a given set of things, each thing would by default have _normal_ value, unless we deliberately mark it _high_ value or _low_ value.
